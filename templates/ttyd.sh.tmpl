#!/usr/bin/env bash
# Generated by webterm-kit installer. Edit install.sh / templates and re-run instead.
exec ttyd -i __BIND_IP__ -p __PORT__ -W \
  -S -C "__CERT__" -K "__KEY__" \
  -t fontSize=18 \
  -t fontFamily=Menlo \
  __CMD__
