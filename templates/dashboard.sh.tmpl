#!/usr/bin/env bash
# Generated by webterm-kit installer. Edit install.sh / templates and re-run instead.
#
# v2: dashboard binds plain HTTP on 127.0.0.1. Caddy on :443 terminates TLS
# and serves it at https://__TAILNET_HOST__/. The chooser URL is path-based
# (https://__TAILNET_HOST__/chooser/) so the SPA can build session links
# without a port number to remember.
exec __DASHBOARD_BIN__ \
  --bind=127.0.0.1 \
  --port=__PORT__ \
  --chooser-url=__CHOOSER_URL__ \
  --playbooks-dir=__PLAYBOOKS_DIR__
